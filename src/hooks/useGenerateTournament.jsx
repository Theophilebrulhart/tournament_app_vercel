
const generateTimePlan = (start, end, fieldNbr) => {
    // console.log("start", start);
}


export function generateTournamentTree({tournament}) {
    console.log("touenament", tournament);
    console.log("s<lue")
    // generateTimePlan(tournament.start, tournament.end, tournament.fieldNbr);
    // separateTeams

    // generateMatchs


}